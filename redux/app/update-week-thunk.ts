import { createAsyncThunk } from "@reduxjs/toolkit";

createAsyncThunk('update/week', 
    async (data, {rejectWithValue})=>{
        try {
            
        } catch (error) {
            
        }
})